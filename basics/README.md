  // DoubleStream coinFlip = doubles();

      // flipNHeads(1);
    // System.out.println("It " + "took" + FLIPS + "flips" + "to" + "flip" + n +
    // "heads" + "in" + "a" + "row.");

    Random rand = new Random();
    int rand_int = rand.nextInt(1);